import React from 'react';

const MovieReviews = props =>
  <div className="review-list">
    <ul>
      {/* return props.reviews.map((review, index) => {
        return <li key={this.index}> </li>
      }) */}
    </ul>
  </div>

export default MovieReviews;
